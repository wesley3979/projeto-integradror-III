const path = require('path')

module.exports = {
  config: path.resolve(__dirname, 'src', 'server', 'config', 'dbConfig.js'),
  'migrations-path': path.resolve(__dirname, 'src', 'server', 'database', 'migrations')
}